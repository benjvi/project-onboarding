stages:
  - deploy

image:
  name: hashicorp/terraform
  entrypoint: [""]

build_a:
  stage: deploy
  script:
    - 'git diff --name-only HEAD~10 | grep deployed-projects | cut -d "/" -f2 | sort | uniq'

